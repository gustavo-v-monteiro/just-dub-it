# ltx-service

Provider-neutral serverless runtime for JustDubit inference.

The package wraps `ltx-pipelines` with:

- a stable job request/result schema
- model bootstrap helpers
- presigned URL input/output helpers
- a warm-worker runtime
- provider adapters for Runpod and Modal

